// L19 - 5. Introducing Variables & Constants

/*

L19 - 5. Introducing Variables & Constants

Variables & Constants
---------------------

- a variable is a data container/ data storage.
- e.g. : let userName = 'Satty'; // creating a variable in js there are three ways let, var and const , then you need to give the name of the variable and then you need to assign the value to the variable.
- you can also reassign the value to the variable except const type variable.
-- e.g. : userName = 'Riya'; 
-- variable sirf pheli bar hi declare kiya jata hai js ko btane ke liye bar bar  variable declare krne ki jarurat nhi hti hai js mein.

-- const totolUsers = 15; // a data conatiner or data storage, where the value must not changed. totolUsers = 15; wrong

Quest the why we need const type variable ? agar humm change ni kar skte hai unko.
const use kro jahan jahan jrurat ho jahan ape m\program mein value badal nahi rahi ho.

Use constants as often as possible (i.e. whenever you actually got data that never changes) to be clear about your intentions (in your code)




*/


console.log("L19 - 5. Introducing Variables & Constants")

// L20 - 6. Declaring & Defining Variables

/*

L20 - 6. Declaring & Defining Variables

Declaring & Defining Variables

-----------------------------

Variable Naming

let userName                  Best Practice: camelCase
let ageGroup5                 Only letters and digits
let $kindOfSpecial            Starting with $ is allowed
let _internalValue            Starting with _ is allowed


Allowed Not Allowed / Not Recommended

let user_name Allowed but bad practice!
let 21Players Starting digits not allowed
let user-b No special characters!
let let Keywordsnot allowed

Note : javascript is case-sensitive in case of variables.

*/


console.log("L20 - 6. Declaring & Defining Variables")